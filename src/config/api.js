export const API_CONFIG = {
  // TODO: replace with your real backend URL
  // Use domain only (no trailing /api) because endpoints include the /api prefix.
  baseUrl: 'https://balmiest-forcipate-mandi.ngrok-free.dev',
  timeoutMs: 15000,
};
